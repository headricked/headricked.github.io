export class Student {
    constructor (firstName, lastName, iNumber, age) {
        this.firstName = firstName,
        this.lastName = lastName,
        this.iNumber = iNumber,
        this.age = age
    }
}